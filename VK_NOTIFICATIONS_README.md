# VK Mini App - Система подписки на уведомления

Готовый JS-код для реализации подписки на push-уведомления в VK Mini App с учетом мобильных устройств.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install @vkontakte/vk-bridge
```

### 2. Базовое использование

```javascript
import bridge from '@vkontakte/vk-bridge';
import { initVKNotifications } from './vk-notifications-simple.js';

// Автоматическая инициализация при загрузке
document.addEventListener('DOMContentLoaded', () => {
    initVKNotifications();
});
```

### 3. Расширенное использование

```javascript
import { initVKNotifications, requestNotificationPermission, showToast } from './vk-notifications-simple.js';

// Инициализация с кастомными сообщениями
initVKNotifications({
    successMessage: 'Спасибо! Теперь вы будете получать уведомления',
    skipMessage: 'Хорошо, подпишемся позже'
});

// Ручной запрос подписки
await requestNotificationPermission('Спасибо за подписку!', 'Можете подписаться позже');

// Показ произвольного уведомления
showToast('Операция выполнена успешно!', 'success');
```

## 📁 Файлы проекта

- **`vk-notifications-simple.js`** - Упрощенная версия для быстрого внедрения
- **`vk-notifications-example.js`** - Полная версия с классом и расширенным функционалом
- **`vk-notifications-demo.html`** - HTML-демонстрация работы системы

## 🔧 Основные функции

### `initVKNotifications(options)`
Инициализирует систему уведомлений и проверяет статус подписки.

**Параметры:**
- `showModal` (boolean) - Показывать ли модальное окно (по умолчанию true)
- `successMessage` (string) - Сообщение при успешной подписке
- `skipMessage` (string) - Сообщение при отказе

### `requestNotificationPermission(successMessage, skipMessage)`
Запрашивает разрешение на отправку уведомлений.

### `showToast(message, type)`
Показывает уведомление пользователю.

**Типы уведомлений:**
- `'success'` - Успех (зеленый)
- `'error'` - Ошибка (красный)
- `'info'` - Информация (синий)

## 📱 Особенности для мобильных устройств

1. **Детекция мобильных устройств** - автоматическое определение мобильных платформ
2. **Упрощенная логика** - если VK Bridge недоступен, используется только база данных
3. **Принудительный показ** - дополнительная проверка через 2 секунды для мобильных
4. **Адаптивный дизайн** - модальные окна оптимизированы для маленьких экранов

## 🎨 Стилизация

Система автоматически добавляет CSS-стили для модальных окон и уведомлений. Стили включают:

- Адаптивный дизайн для мобильных устройств
- Анимации появления и исчезновения
- Градиентные кнопки и фоны
- Поддержка темной темы

## 🔍 Обработка ошибок

Система обрабатывает следующие типы ошибок:

1. **`client_error`** - Метод не поддерживается в старой версии клиента
2. **`api_error`** - Ошибка API ВКонтакте
3. **Сетевые ошибки** - Проблемы с подключением
4. **Ошибки VK Bridge** - Недоступность моста

## 📊 Логирование

Все действия системы логируются в консоль с эмодзи для удобства отладки:

```javascript
🔔 Инициализация системы уведомлений...
👤 Пользователь: Иван Петров
📊 Статус подписки: ✅ Разрешено
🔔 Запрашиваем разрешение на уведомления...
📱 Результат запроса: {result: true}
✅ Пользователь успешно подписался
```

## 🧪 Тестирование

Для тестирования откройте файл `vk-notifications-demo.html` в браузере. Демо включает:

- Мок VK Bridge для имитации API
- Интерактивные кнопки для тестирования функций
- Визуальные индикаторы статуса
- Примеры кода

## 📋 Примеры использования

### Автоматическая подписка при первом заходе

```javascript
// В главном файле приложения
import { initVKNotifications } from './vk-notifications-simple.js';

// Инициализация при загрузке страницы
document.addEventListener('DOMContentLoaded', () => {
    initVKNotifications({
        successMessage: 'Спасибо! Теперь вы будете получать важные уведомления',
        skipMessage: 'Хорошо, подпишемся позже в настройках'
    });
});
```

### Ручная подписка по кнопке

```javascript
// В обработчике кнопки
document.getElementById('subscribe-btn').addEventListener('click', async () => {
    try {
        await requestNotificationPermission(
            'Спасибо за подписку!',
            'Вы можете подписаться позже'
        );
    } catch (error) {
        console.error('Ошибка подписки:', error);
    }
});
```

### Проверка статуса подписки

```javascript
// Проверяем, подписан ли пользователь
async function checkSubscriptionStatus() {
    try {
        const result = await bridge.send('VKWebAppCheckAllowedNotifications');
        return result.result;
    } catch (error) {
        console.error('Ошибка проверки статуса:', error);
        return false;
    }
}
```

## 🔧 Настройка

### Кастомизация сообщений

```javascript
initVKNotifications({
    successMessage: '🎉 Отлично! Теперь вы будете получать уведомления',
    skipMessage: '👌 Хорошо, подпишемся позже в настройках'
});
```

### Отключение модального окна

```javascript
// Только проверка статуса без показа окна
initVKNotifications({ showModal: false });
```

## 📱 Мобильная оптимизация

Система автоматически определяет мобильные устройства и применяет специальную логику:

- Упрощенная проверка статуса
- Принудительный показ окна через 2 секунды
- Адаптивные размеры модальных окон
- Оптимизированные анимации

## 🚨 Важные замечания

1. **VK Bridge** должен быть инициализирован перед использованием
2. **Мобильные устройства** могут иметь ограничения VK Bridge
3. **Пользователь может отказаться** - всегда обрабатывайте отказы
4. **Тестируйте на реальных устройствах** - эмуляторы могут работать по-другому

## 📞 Поддержка

При возникновении проблем проверьте:

1. Консоль браузера на наличие ошибок
2. Поддержку VK Bridge в версии клиента
3. Настройки уведомлений в браузере
4. Сетевые ограничения

## 📄 Лицензия

Код предоставляется "как есть" для использования в VK Mini App проектах.



