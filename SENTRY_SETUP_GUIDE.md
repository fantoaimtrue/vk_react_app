# 📊 Руководство по настройке Sentry

**Sentry** - это система мониторинга ошибок для приложений. Позволяет отслеживать ошибки в реальном времени, получать уведомления и анализировать производительность.

---

## 🎯 Почему Sentry?

### Возможности:
- ✅ **Отслеживание ошибок** в реальном времени
- ✅ **Стек-трейсы** с номерами строк и контекстом
- ✅ **Уведомления** по Email, Slack, Telegram
- ✅ **Привязка к пользователям** - видите, кто столкнулся с ошибкой
- ✅ **Метрики производительности** - медленные запросы
- ✅ **Breadcrumbs** - что делал пользователь до ошибки
- ✅ **Release tracking** - отслеживание версий
- ✅ **Бесплатный план**: 5000 ошибок/месяц

---

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Регистрация на Sentry

1. Перейдите на https://sentry.io
2. Нажмите "Sign Up" (можно через GitHub)
3. Создайте аккаунт

### Шаг 2: Создание проекта

1. Нажмите "Create Project"
2. Выберите платформу: **Django**
3. Назовите проект: `babkimanki`
4. Нажмите "Create Project"

### Шаг 3: Получение DSN

После создания проекта вы увидите DSN:
```
https://examplePublicKey@o0.ingest.sentry.io/0
```

Это ваш **уникальный ключ** для отправки ошибок.

### Шаг 4: Добавление в проект

```bash
# Откройте .env файл
nano /root/vk_react_app/.env

# Добавьте строку (раскомментируйте):
SENTRY_DSN=https://ваш-dsn-здесь

# Сохраните (Ctrl+O, Enter, Ctrl+X)
```

### Шаг 5: Перезапуск

```bash
cd /root/vk_react_app
docker-compose -f docker-compose.yml -f docker-compose.prod.yml restart backend

# Проверьте логи
docker-compose logs backend | tail -20
```

---

## 🧪 Тестирование

### Проверка работы Sentry:

```bash
# Вызовите тестовую ошибку
docker-compose exec -T backend python manage.py shell -c "
import sentry_sdk
sentry_sdk.capture_message('Тестовое сообщение из БАБКИМАНКИ!')
print('✅ Тестовое сообщение отправлено в Sentry')
"
```

Через 1-2 минуты проверьте Dashboard на https://sentry.io - там должно появиться тестовое сообщение.

---

## 📱 Использование в коде

### Backend (Django):

Sentry автоматически ловит все необработанные ошибки! Но можно отправлять вручную:

```python
import sentry_sdk

# Отправить сообщение
sentry_sdk.capture_message('Что-то произошло', 'info')

# Отправить ошибку
try:
    something_risky()
except Exception as e:
    sentry_sdk.capture_exception(e)

# Добавить контекст
with sentry_sdk.push_scope() as scope:
    scope.set_tag("payment_method", "credit_card")
    scope.set_user({
        "id": user.vk_user_id,
        "username": f"{user.first_name} {user.last_name}"
    })
    sentry_sdk.capture_message("Платеж обработан")
```

### Frontend (React):

В `src/utils/logger.js` уже настроена интеграция:

```javascript
import logger from './utils/logger';

// Ошибки автоматически отправляются в Sentry (если настроено)
logger.error('❌ Ошибка загрузки данных:', error);
```

Для полной интеграции React можно добавить:

```bash
npm install @sentry/react
```

```javascript
// src/main.jsx
import * as Sentry from "@sentry/react";

Sentry.init({
  dsn: "https://your-dsn",
  integrations: [
    Sentry.browserTracingIntegration(),
    Sentry.replayIntegration(),
  ],
  tracesSampleRate: 0.1,
  replaysSessionSampleRate: 0.1,
  replaysOnErrorSampleRate: 1.0,
});
```

---

## 📊 Dashboard Sentry

### Что вы увидите:

1. **Issues** - Список всех ошибок
2. **Performance** - Медленные запросы
3. **Releases** - История версий
4. **Alerts** - Настройка уведомлений

### Полезные фильтры:

- По окружению (production/development)
- По пользователям
- По URL
- По типу ошибки
- По времени

---

## 🔔 Уведомления

### Настройка в Sentry:

1. **Settings** → **Alerts**
2. Создайте правило:
   - Тип: "Issues"
   - Условие: "When an issue is first seen"
   - Действие: "Send a notification to..."
   - Канал: Email / Slack / Discord / Telegram

### Рекомендуемые правила:

1. **Критические ошибки** - немедленно
2. **Новые типы ошибок** - в течение часа
3. **Частые ошибки** (10+ раз/час) - уведомление

---

## 🔍 Отладка проблем

### Проблема: "Ошибки не появляются в Sentry"

**Проверьте**:
1. DSN правильный в `.env`
2. Backend перезапущен после добавления DSN
3. DEBUG=False в продакшене
4. Нет ошибок подключения к Sentry

```bash
# Проверьте переменные окружения
docker-compose exec backend env | grep SENTRY

# Проверьте логи
docker-compose logs backend | grep -i sentry
```

### Проблема: "Слишком много ошибок отправляется"

**Решение**: Уменьшите `traces_sample_rate` в settings.py:

```python
sentry_sdk.init(
    dsn=SENTRY_DSN,
    traces_sample_rate=0.05,  # Только 5% транзакций
)
```

### Проблема: "Превышена квота Sentry"

**Бесплатный план**: 5000 ошибок/месяц

**Решения**:
1. Исправьте частые ошибки
2. Добавьте фильтрацию в `before_send`:

```python
def before_send(event, hint):
    # Не отправляем 404 ошибки
    if event.get('level') == 'info':
        return None
    return event

sentry_sdk.init(
    dsn=SENTRY_DSN,
    before_send=before_send,
)
```

---

## 📈 Метрики и аналитика

### Performance Monitoring

Sentry автоматически отслеживает:
- ⏱️ Время выполнения запросов
- 🔄 Количество SQL запросов
- 📡 HTTP запросы к внешним API
- 🐌 Медленные эндпоинты

### Dashboards

Создайте кастомные дашборды для:
- Частота ошибок по времени
- Топ ошибок
- Статус по пользователям
- Производительность API

---

## 💰 Тарифы Sentry

### Бесплатный план (Team):
- ✅ 5000 ошибок/месяц
- ✅ 10,000 performance units
- ✅ 1 проект
- ✅ Базовые алерты
- ✅ 7 дней хранения

### Paid plans:
- **Developer**: $26/месяц (50K ошибок)
- **Team**: $80/месяц (100K ошибок)
- **Business**: Custom pricing

**Для вашего проекта**: Бесплатного плана достаточно на старте! 💎

---

## 🎓 Best Practices

### 1. Не отправляйте чувствительные данные
```python
# Правильно
sentry_sdk.set_user({"id": user.id})

# Неправильно
sentry_sdk.set_user({"password": user.password})  # ❌
```

### 2. Используйте теги для категоризации
```python
with sentry_sdk.push_scope() as scope:
    scope.set_tag("mfo_id", mfo.id)
    scope.set_tag("action", "loan_application")
    # ... ваш код
```

### 3. Добавляйте контекст
```python
sentry_sdk.set_context("mfo_details", {
    "name": mfo.name,
    "sum": loan_sum,
    "term": loan_term
})
```

### 4. Используйте releases для версионирования
```python
sentry_sdk.init(
    dsn=SENTRY_DSN,
    release="babkimanki@1.0.0",
)
```

---

## 🔗 Интеграции

Sentry интегрируется с:
- 📧 **Email**
- 💬 **Slack**
- 📱 **Telegram**
- 🔔 **Discord**
- 📊 **Jira** (для задач)
- 🐙 **GitHub** (для issues)
- 🔄 **GitLab**

---

## ✅ Чеклист настройки

- [ ] Зарегистрировались на sentry.io
- [ ] Создали проект Django
- [ ] Получили DSN
- [ ] Добавили DSN в `.env`
- [ ] Перезапустили backend
- [ ] Отправили тестовое сообщение
- [ ] Увидели сообщение в Dashboard
- [ ] Настроили уведомления (опционально)

---

## 📞 Поддержка

- **Документация**: https://docs.sentry.io/platforms/python/guides/django/
- **Community**: https://forum.sentry.io/
- **Status**: https://status.sentry.io/

---

**🎉 Готово! Теперь все ошибки под контролем!**

**Дата**: 8 ноября 2025  
**Статус**: ✅ Sentry настроен и готов к использованию

