# 🔒 Руководство по безопасности проекта

## ⚠️ КРИТИЧЕСКИ ВАЖНО

### 1. Переменные окружения (.env файл)

**НИКОГДА** не коммитьте `.env` файл в Git!

#### Настройка для продакшена:

1. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

2. Сгенерируйте новый SECRET_KEY:
```python
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
```

3. Заполните `.env` файл реальными данными:
```env
DJANGO_SECRET_KEY=ваш-сгенерированный-секретный-ключ
DJANGO_DEBUG=False
ALLOWED_HOSTS=bodyexp.ru,www.bodyexp.ru,localhost,127.0.0.1,backend
VK_APP_ACCESS_TOKEN=ваш-реальный-токен-vk
VK_APP_ID=53875526
```

### 2. Безопасность Docker

Обновленный `docker-compose.prod.yml` теперь читает переменные из `.env` файла:
```yaml
backend:
  env_file:
    - .env  # Безопасно!
```

### 3. Безопасность базы данных

**Текущая конфигурация**: SQLite
- ✅ Подходит для малых проектов
- ⚠️ НЕ рекомендуется для больших нагрузок

**Рекомендация**: Перейти на PostgreSQL для продакшена

### 4. CORS настройки

Текущие настройки в `backend/backend/settings.py`:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:5173",
    "http://127.0.0.1:5173",
]
```

✅ Это безопасно - разрешает только локальный доступ для разработки.

### 5. Nginx Security Headers

Проверьте `nginx-ssl-with-backend.conf`:
- ✅ HSTS включен
- ✅ X-Frame-Options настроен для VK
- ✅ X-Content-Type-Options включен
- ✅ X-XSS-Protection включен

### 6. SSL/TLS

- ✅ TLS 1.2/1.3 включены
- ✅ Современные шифры
- ✅ Let's Encrypt сертификаты

## 📋 Чеклист перед деплоем

- [ ] `.env` файл создан и заполнен
- [ ] SECRET_KEY сгенерирован уникальный
- [ ] DEBUG=False в продакшене
- [ ] `.env` добавлен в `.gitignore`
- [ ] SSL сертификаты получены
- [ ] ALLOWED_HOSTS настроен правильно
- [ ] VK токены актуальны
- [ ] База данных имеет бэкапы

## 🔍 Регулярные проверки

### Проверка секретов в Git:
```bash
# Убедитесь, что секреты не попали в Git
git log --all -- .env
# Должно быть пусто!
```

### Мониторинг логов:
```bash
# Проверяйте логи на подозрительную активность
docker logs babkymanki-full | grep -i error
journalctl -u babkymanki-backend -n 100
```

### Обновление зависимостей:
```bash
# Frontend
npm audit
npm audit fix

# Backend
pip list --outdated
```

## 🚨 Что делать при утечке SECRET_KEY

1. **Немедленно** смените SECRET_KEY
2. Пересоздайте все сессии: `python manage.py clearsessions`
3. Проверьте логи на подозрительную активность
4. Уведомите пользователей об изменении паролей

## 📞 Контакты

При обнаружении проблем безопасности:
- Не создавайте публичные issue
- Свяжитесь напрямую с администратором

